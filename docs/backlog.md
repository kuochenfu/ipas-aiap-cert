# Backlog

> 2026-08-17 全面複盤，同日執行 T1–T6。每一項都在複盤當日重新量測過，數字附在條目內；
> 未經量測的舊條目已刪除或標註。內容正確性的人工複審缺口不在此文件，
> 見 `docs/coverage/` 各檔與 auto-memory 的待複審清單。
>
> **狀態：T1–T7 已完成、T8 完成一半（2026-08-17）。** 各節開頭標示了完成狀態與實測結果。
> T8 剩下的「中級三科各 100 題新題」是獨立的內容工程，另行排程。

## 複盤結論摘要

| 舊條目 | 複盤結果 |
|---|---|
| 中級 guide 詳解殘留「解析：」前綴 | **仍是問題**，且與下一項是同一批題 → 合併為 T2 |
| 中級三科幾乎無手寫選項解析 | **仍是問題**，最嚴重的 110 題與上一項完全重疊 → 合併為 T2 |
| 錯題本只寫不讀 | **仍是問題**，但性質是「要接上還是要刪掉」的決策 → T3 |
| `topic` 全部未分類 | **仍是問題**，已量化影響 → T7 |
| practice bank 中級三科未建置 | **仍是問題** → T8 |
| 依評鑑節點檢視作答表現 | **仍是問題**，且範圍比想像大（practice 模式完全沒有彙總視圖）→ T8 |
| 新題補到與真題同數 | **已不是問題，刪除**（見下方說明） |
| 無障礙待加強 | **仍是問題**，已收斂為兩個具體項 → T6 |
| 解析器健檢測試 | **仍是問題**，本次新增三個實例證據 → T4 |
| 低信心詳解回頭清 | **範圍比記錄的小得多**：實際只有 2 題 → T5 |
| `deploy.yml` 升 Node 24 | **已不是問題，刪除**（見下方說明） |
| — | **新發現：CI 從不跑測試** → T1 |

### 兩項已刪除的條目

**「新題補到與真題同數（junior 100、senior 50）」——目標已由其他方式達成。**
這個目標訂於 2026-06-01，理由寫得很清楚：題庫太小，三份模擬試卷只是同一集合的排列。
複盤時直接量三份試卷的重疊度：

| 科目 | 題庫 | 三份兩兩重疊（/50） | 三份共涵蓋 |
|---|---|---|---|
| junior-ai-basics | 222 | 15 / 7 / 12 | 117 題 |
| junior-genai | 213 | 10 / 12 / 13 | 119 題 |
| senior-ai-tech | 154 | 16 / 18 / 19 | 104 題 |
| senior-bigdata | 164 | 14 / 8 / 12 | 116 題 |
| senior-ml | 164 | 12 / 17 / 16 | 109 題 |

重疊 16–38%，三份合計涵蓋 104–119 題。原始理由已滿足——而且滿足它的不是新題，
是真題本身長大了（初級 190/179、中級 130–140）。若仍要擴充題量，需要一個**新的**
理由（例如評鑑節點覆蓋），而那個需求已由 practice bank 承接 → 併入 T8。

**「`deploy.yml` 升 Node 24」——誤診。**
最近 5 次部署全部成功（最新 2026-08-14）。`node-version: 20` 是**建置用**的 Node 版本，
是自由選擇，與 GitHub Actions 執行環境的棄用時程無關；工作流程用的
`checkout@v4`／`setup-node@v4`／`upload-pages-artifact@v3`／`deploy-pages@v4` 都是現行版本。
此條目源自對 Actions 棄用公告的誤讀，刪除。（但檢視同一個檔案時發現了 T1。）

---

## T1. 讓 CI 跑測試　✅ 已完成（2026-08-17）

**現況**：`.github/workflows/deploy.yml` 只有 `npm ci` → `npm run build`。
**全專案 225 個測試從來沒有在 CI 跑過。**

這使得所有既有守衛——資料完整性、詳解覆蓋率、簡體字檢查、節點配額、以及本次新增的
`tests/figures.test.ts`——都只在有人記得在本機跑 `npm run test` 時才生效。一次忘記，
壞掉的資料就直接部署上線。

專案在 2026-08-14 的回顧裡寫過「留到最後才補的守衛，一條都攔不到」；這裡的情況更嚴重：
守衛補了，但沒有接上扳機。

**做法**：在 `build` job 的 `npm run build` 之前插入 `npm run test`。
**結果**：已加入 `.github/workflows/deploy.yml`；244 個測試現在每次推 `main` 都會跑，不過就不部署。

---

## T2. 修復中級三科的 110 題學習指引參考題　✅ 已完成（2026-08-17）

**現況**：`senior-ai-tech` 30 題、`senior-bigdata` 40 題、`senior-ml` 40 題，
**110 題全部**同時壞在兩個地方——複盤時逐題比對，重疊是 110/110，不是兩批題目而是同一批。

**壞法一：詳解開頭黏著解析管線的殘留。** 正解選項的原文被黏在詳解最前面，
接著一個沒被切掉的「解析：」標記：

- `senior-ml-guide-q002`：「**0 或 1解析：**伯努利分佈是典型的二元分佈⋯」
- `senior-bigdata-guide-q001`：「**概括並整理資料特徵解析：**敘述統計主要用於⋯」
- `senior-ai-tech-guide-q001`：「**Word2Vec解析：**Word2Vec 是一種詞嵌入方法⋯」

（不是答案外洩——詳解只在揭曉後顯示——但每一則都以一段語意不通的碎片開場。）

**壞法二：三條選項解析全是通用填充。** 這 110 題的 330 條選項解析，
**100% 命中**那句「此選項不是本題答案；它描述的是「⋯」，但本題正解應判斷為⋯」。

**複盤時的額外發現**——把通用填充率依批次拆開，落差很大：

| 批次 | 題數 | 通用填充率 |
|---|---|---|
| guide（舊） | 110 | **100%** |
| `gen-*` 新題（舊） | 72 | 98% |
| 114-2 真題（舊） | 150 | 45% |
| 115-1 真題（2026-08-17 新寫） | 150 | **24%** |

後備機制會先嘗試從詳解裡抽出「談某個選項」的片段，抽不到才用填充句。
落差來自**詳解的句型**：115-1 那批逐項寫成「A 說⋯；B 把⋯；C 的⋯」，抽取器接得住。
所以要降低填充率，不一定要為每題手寫三條選項解析——**先讓詳解逐項點名，就能自動吃掉一半以上**。
這對後續批次是可重用的槓桿。

**結果**：110 題詳解全數手寫重寫，殘留歸零。重寫時另發現兩類先前未記錄的損壞——
每科最後一題被併入整段章節前言（各數百字），以及 `senior-ml-guide-q032` 的答案區塊錯位
（詳解講的是另一題的內容）。細節見 `docs/coverage/bank-defects.md`。

連帶把「壞法二」一併解決，且效果超出這 110 題：改採逐項點名句型後，再擴充 `render.ts`
的後備抽取器（認得括號寫法與連接詞串接、支援一個子句同時解釋兩個選項），
中級三科的通用填充率從 **59% 降到 5%**——guide 批 100%→0%、114-2 45%→11%、
`gen-*` 98%→7%、115-1 24%→2%。三條抽取器行為都有測試並經突變驗證。

---

## T3. 錯題本：接上讀取端，或刪掉它　✅ 已完成（2026-08-17，選擇「接進刷題篩選器」）

**現況**（複盤時查證）：

- `src/state/storage.ts` 匯出四個函式，`tests/storage.test.ts` 四個都測了。
- 生產程式碼中**只有 `addMiss` 有呼叫端**（`main.ts:422`，在 `finishExam()` 內）。
  `loadMisses`、`isMissed`、`removeMiss` 在 `src/` 底下零呼叫。
- 刷題模式的「錯題」篩選器**不使用**這個 store，它讀的是 `drillProgress` 的作答紀錄
  （`src/domain/drill.ts` 的 `drillMatches`）。

所以 `ipas-aiap-misses` 裡唯一存在的東西是**模擬考的錯題**，累積至今、無人讀取、也無法清除。

**而 `CLAUDE.md` 有一條不變式在保護它**：「錯題本 localStorage key 為 `ipas-aiap-misses`，
勿更名（會遺失現有使用者紀錄）」——保護的是一份沒有讀取端的資料。

**已於 2026-08-17 選定「接進刷題篩選器」並實作**：`drillMatches` 的 `wrong` 現在把
「本次刷題答錯」與「在錯題本裡且本次刷題尚未作答」兩者聯集；刷題答對即從篩選中消失
（使用者消掉錯題的途徑），「重置進度」則連同本科目的錯題本一併清除（`removeMiss` 因此有了呼叫端）。
純函式的 `missed` 參數為選用，未傳時行為與併入前相同。實測：模擬考錯 48 題後進刷題，
「錯題」篩選器即顯示 48。

當初評估的兩個方向：

- **接上**：模擬考錯題跨場次累積，確實是刷題篩選器涵蓋不到的價值（篩選器只看本次刷題進度）。
  需要一個入口與一個瀏覽/清除介面。
- **刪掉**：連同 `storage.ts`、其測試與 `CLAUDE.md` 的不變式一起移除，
  少一個沒人維護的儲存格式。既然從未有使用者看過這份資料，刪除的成本近乎零。

---

## T4. 把解析器的靜默損壞固化為測試　✅ 已完成（2026-08-17）

**現況**：`npm run parse:papers` 只有兩條警告——「解析出 N 題（預期 50）」與「有空白選項」。
2026-08-17 匯入新考卷時觸發的**三個**缺陷，一條警告都沒觸發，因為它們產生的是
「看起來合理」的輸出（選項有文字、題數正確，只是多了一截）：

| 缺陷 | 症狀 |
|---|---|
| 欄位標題順序相反（「題目 答案」） | 標題黏進題幹 |
| 「二、程式題」區塊標題未列雜訊 | 黏進**前一題**最後一個選項 |
| 共用引文被斷成兩行 | 下一組題目的整段前言被併進前一題選項 |

三者都讓欄位**變長**，因此可偵測。發現它們靠的是把 150 題印出來人眼掃，不可規模化。

**結果**：新增 `tests/parseResidue.test.ts`。實作時發現「選項長度離群」這條偵測力太弱
（三個缺陷附加的文字有的只有 5 個字），改以**內容標記**偵測：跨題共用引文、區塊與欄位標題、
學習指引解析標記、章節頁尾、選項尾端題號。**三個缺陷逐一重新注入解析器驗證過，全部被攔下。**
「章節頁尾」的第一版正則會把「提供 3-5 個案例」誤判，已收緊為僅比對欄位結尾。

---

## T5. 兩則自陳未經驗證的詳解　✅ 已完成（2026-08-17）

複盤時掃全題庫的「低信心／依官方答案／附圖缺失」標記，**實際只有 2 題**
（另 2 題命中是誤判——`senior-ai-tech-115-1-q27` 與 `junior-genai-gen-q027`
的「低信心」出現在題目內容裡，不是自陳標記）。舊條目說的「回頭清一遍」範圍被高估了。

- **`senior-ml-114-2-q49`**：已依補上的圖重寫，參數量由圖直接推算
  （9×10+10=100、10×10+10=110，與圖中第三層的 11 互相印證）。修的過程另外發現
  該題選項 (A) 的數學式也是圖片、文字被截斷成「activation="relu"其數學式為 」，
  已補為 `choiceFigures`。**這也讓 `resolveExplanations` 的合併規則改了**：
  原本只在選項文字為空時才套用轉錄，現改為「有轉錄就以轉錄為準」——
  因為被圖截斷的殘句是非空的，舊規則接不住。
- **`senior-ml-114-2-q17`**：詳解自己承認「A 亦含動量」，判準沒真的把 A 排除掉。
  **已重寫**：改用「動量是演算法定義的一部分，還是可以關掉的選用參數」這條判準——
  Adam 的一階動量無法關閉；PyTorch 的 `SGD` 與 `RMSprop` 都以 `momentum=0` 為預設、屬選用；
  Adagrad 完全沒有動量項。這條判準對四個選項同時成立，不再是瑕疵題。

---

## T6. 無障礙：兩個具體項　✅ 已完成（2026-08-17）

舊條目「鍵盤操作、aria 標記、焦點管理待加強」複盤後收斂——
`index.html` 已有 `lang="zh-Hant"`，選項都是真的 `<button>`（原生可鍵盤操作），
`aria-label`／`aria-pressed` 在篩選器與朗讀按鈕上都有。剩下兩個真缺口：

- **計時器與「已作答」計數就地更新，但沒有 `aria-live`。**
  `main.ts:146` 與 `main.ts:518` 直接改 `.timer` 與 `.answered-count` 的內容。
  在一個**有時間限制**的考試裡，螢幕閱讀器使用者完全不會被告知剩餘時間。
- **換頁後沒有焦點管理。** 每次 `app.innerHTML = ...`（`main.ts` 有十餘處）都會清掉焦點，
  鍵盤與螢幕閱讀器使用者每次導覽都被丟回文件開頭。

**結果**：
- 計時器改為 `role="timer"` ＋ `aria-label`，並**刻意不加 `aria-live`**（`role="timer"` 依規範隱含
  `aria-live="off"`；逐秒播報會蓋掉正在朗讀的題目）。改由 `main.ts` 在剩 30/10/5/1 分鐘與時間到時，
  寫入新增的視覺隱藏區 `.sr-live`。已作答計數則用 `aria-live="polite"`——它只在使用者作答時變動。
- 焦點管理只在**畫面切換**時把焦點移到主標題；同一畫面內的重繪（作答、切篩選、翻頁）
  改為把焦點還給重繪前的那顆按鈕。實作時發現一個先前未記錄的缺陷：**刷題每作答一次，
  整張題卡被 `innerHTML` 重建，焦點直接掉回 `<body>`**——鍵盤使用者每答一題就失去位置。
  現在焦點會留在剛作答的選項上（該選項同時帶著正誤狀態），這比原本的缺口更常觸發。

---

## T7. 回填 `topic`　✅ 五科全部完成（2026-08-17）

**現況**：五科 **779 題真題全部**是「未分類」；只有 `gen-*` 新題帶主題名稱。
影響已量化——模擬考成績頁的主題統計實際長這樣：

```
senior-ml 第 1 份：  未分類: 42/42 ／ 機器學習治理: 3/3 ／ 機器學習與深度學習: 2/2
                    ／ 機器學習基礎數學: 2/2 ／ 機器學習建模與參數調校: 1/1
```

84% 的題目落在單一分類，這個表格等於沒有作用。

**注意**：`render.ts:252` 的主題徽章條件是 `/^L\d{5} /`，而原題庫的主題是名稱（如「機器學習概念」）
而非節點碼，所以徽章在原題庫**從不顯示**；practice bank 的 100/100 題都帶
`L11202 資料整理與分析流程` 這種節點碼，徽章在那邊是正常運作的。
回填時應直接採節點碼格式，兩邊才一致。

**結果（中級三科）**：410 題真題 ＋ 72 題新題全部回填為五碼節點，「未分類」歸零。

回填的起點比預期好——**409/410 題的詳解裡已經帶有三碼主題標註**（寫詳解時逐題標的），
所以工作量從「讀 410 題判斷主題」縮成「在 2–4 個節點中挑一個」。分類寫在手寫層
`explanations/*.ts` 的 `topic` 欄位，經 `resolvePastExamExplanations` 合併，重跑解析不會洗掉；
新題的 topic 則直接改在 `generated/*.ts` 的題目物件內。

同時把 `gen-*` 的三碼主題名稱一併換成五碼節點——否則成績頁會把
「L23401 數據隱私、安全與合規」與「機器學習治理」拆成兩列，看起來像兩個不同的東西。

成績頁的實際效果：原本 `未分類: 42/42` 一列吃掉 84%，現在是 11–13 列的節點分布。

**撞號的處理**：官方《評鑑內容範圍參考》在 `L233` 底下把第三、四個節點編為
`L22303`／`L22304`（應為 `L23303`／`L23304`），而 `L22303` 已被 `L223` 的「數據可視化工具」用掉。
本專案採**修正後**的編號，理由是節點碼在此是分組用的唯一鍵，沿用官方編號會讓兩個科目的
節點合併成同一列。決定與理由寫在 `assessmentTopics.ts` 的註解，
並由 `tests/topicBackfill.test.ts` 的「節點碼全體唯一」擋住回歸。

**初級兩科（同日補做）**：369 題真題 ＋ 66 題新題也全部回填完畢。初級沒有中級那種
「詳解已帶 L 碼」的捷徑，是逐題重新判斷的。至此**全題庫 917 題無一「未分類」**。

初級回填後的連帶效果：主題徽章與「節點表現」視圖對初級原題庫也生效了
（原本只有新題庫與中級原題庫有）。`tests/topicBackfill.test.ts` 已擴及五科，
並要求「目錄裡每個節點至少有一題」，避免分類全擠在少數節點。

---

## T8. practice bank 擴充到中級三科 ＋ 節點統計視圖　◐ 節點統計視圖已完成；中級新題未做

**現況**：`practiceTopics` 只有 `junior-ai-basics`、`junior-genai`，各 100 題。
中級三科沒有「新題庫練習」入口。

**而且範圍比舊記錄大**：複盤時查證，practice 模式**完全沒有任何彙總視圖**——
不只是「依節點檢視作答表現」沒做，是連成績頁都沒有，只有逐題刷題。
節點分類目前只存在於資料與單題徽章上。

這一項同時吸收了被刪除的「新題補到滿額」——若要繼續擴充題量，
理由應該是評鑑節點覆蓋，而承接機制就是 practice bank。

**已完成：節點統計視圖。** 刷題頁新增「節點表現」入口（`data-nav="topics"`），
列出各評鑑節點的「已作答 / 總題數」與答對率。設計上刻意把「已作答」與「答對」分開呈現——
刷題可以只做一部分，只給答對率會讓「做了一題且答對」看起來是滿分，
而尚未作答的節點顯示「尚未作答」而非 0%（0% 會被讀成全錯）。

入口採條件顯示：只有題庫**過半數題目帶五碼節點碼**時才出現。T7 完成後五科都已分類，
因此原題庫與新題庫都看得到；這個閘門保留下來，是為了日後若再匯入未分類的新來源時
不會顯示一張全是「未分類」的表。

**仍未做：中級三科各 100 題新題（共 300 題）。** 這是全新命題＋詳解＋選項解析，
依新題庫的前例需要多輪撰寫與審查，且產出仍是 LLM 命題、要進待複審清單，
與本批的其他工作不是同一個量級，另行排程。

---

## 建議順序

1. **T1**（一行，解除所有既有守衛的空轉）
2. **T5 的 q49**（5 分鐘，同類缺陷的漏網之魚）
3. **T3 決策**（不動手，先決定接上還是刪掉）
4. **T4**（小，且能防止 T2 的成果日後被解析器再次污染）
5. **T6**（小，兩處）
6. **T2**（大，但是唯一確定的使用者面缺陷）
7. **T7 → T8**（大，需先解決節點編號問題）

以上於 2026-08-17 依序執行完畢，唯 T7 的初級兩科與 T8 的 300 題新題留待後續。

---

## 2026-08-21 新增：AIoT 應用工程師（初級）

本站於本日納入第二張證照。首批只做**物聯網類**（考科一 AIoT 基礎概論 ＋ 考科二 物聯網系統與應用），
兩科皆學科；術科的考科三、考科四不做——目前不考，且無法以單選題呈現。

### A1. AIoT 考科二尚無任何題源

考科二**沒有官方學習指引**（只有簡章 2.5 節的兩層評鑑內容大綱），也還沒有公告真題。
目前在科目頁以「尚無題目」呈現、不可點入；`tests/data.test.ts` 的 `KNOWN_EMPTY` 名單守住這個狀態——
名單外的科目若題庫變空仍會測試失敗。

承接方式有二：等官方釋出考科二學習指引（屆時走同一條 `parseStudyGuide` 管線，
只需在 manifest 加一筆並補 sections），或比照 practice bank 自行命題。
後者是內容工程，需要複審流程，與本批不是同一量級。

### A2. AIoT 的題數與配分未查證，因此沒有模擬考試

115 年度簡章只寫「每科目單選題、每科 100 分、四捨五入取整數、70 分及格」，
**未載明題數與每題配分**。現行 `examRules` 是 50 題 × 2 分，直接套用會形成錯誤預期，
因此 AIoT 兩科的 `Subject.mockExam` 為 `false`，模式頁只顯示刷題並附一行說明。

解除條件：拿到官方公告題數（或一份真題）後，把 `examRules` 改為依科目查表，再把 `mockExam` 打開。

### A3. AIoT 考科一的選項解析多數仰賴後備抽取器

80 題中只有 11 題自帶逐項選項解析，其餘 69 題是整體詳解散文，且多數未逐項點名 A/B/C/D，
因此 `render.ts` 的後備抽取器會落到通用填充句——與中級三科在 T2 之前的處境相同。
若要改善，作法比照 T2：手寫選項解析，或改寫詳解使其逐項點名。

### A4. AIoT 的學習主題頁只有官方大綱

`studyGuide.ts` 的 AIoT 兩科目前只有評鑑內容大綱，`notes` 與 `links` 皆為空。
`tests/studyGuide.test.ts` 以 `subject.cert === "aiot"` 放行這兩科——補上筆記與延伸閱讀後，
把該條件移除，斷言就會重新守住它們。
